- name: Enable CBT on all compatible disks across VMs
  hosts: localhost
  gather_facts: false
  vars:
    vm_list: []
    snapshot_name: "cbt-activation"
    vm_folder: "/"

  tasks:

    - name: Assert required variables are provided
      assert:
        that:
          - vm_list | length > 0
          - dc_name is defined and dc_name | length > 0
          - vcenter_hostname is defined
          - vcenter_username is defined
          - vcenter_password is defined
        fail_msg: >-
          Required variables missing. Ensure vm_list (non-empty), dc_name,
          vcenter_hostname, vcenter_username, and vcenter_password are all set.

    - name: Enable CBT for each VM
      include_tasks: cbt-enable-per-vm.yml
      loop: "{{ vm_list }}"
      loop_control:
        loop_var: vm_name
        label: "{{ vm_name }}"
